using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scrpits : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
         Debug.Log("AfroGames");  
    
    }

    // Update is called once per frame
    void Update()
    {
          
    }
}
